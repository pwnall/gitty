<%= form_for(@config_flag) do |f| %>
  <% if @config_flag.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@config_flag.errors.count, "error") %> prohibited this config_flag from being saved:</h2>

      <ul>
      <% @config_flag.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :value %><br />
    <%= f.text_field :value %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
