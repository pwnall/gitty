<h1>Listing config_flags</h1>

<table>
  <tr>
    <th>Name</th>
    <th>Value</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @config_flags.each do |config_flag| %>
  <tr>
    <td><%= config_flag.name %></td>
    <td><%= config_flag.value %></td>
    <td><%= link_to 'Show', config_flag %></td>
    <td><%= link_to 'Edit', edit_config_flag_path(config_flag) %></td>
    <td><%= link_to 'Destroy', config_flag, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Config flag', new_config_flag_path %>
