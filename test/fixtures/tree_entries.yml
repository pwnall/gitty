hello_gitmodules:
  tree: hello_root
  child: gitmodules (Blob)
  name: .gitmodules
  
hello_lib:
  tree: hello_root
  child: hello_lib (Tree)
  name: lib

hello_lib_ghost:
  tree: hello_lib
  child: lib_ghost (Tree)
  name: ghost

hello_lib_markdpwn:
  tree: hello_lib
  child: markdpwn_012 (Submodule)
  name: markdpwn

lib_ghost_hello_rb:
  tree: lib_ghost
  child: lib_ghost_hello_rb (Blob)
  name: hello.rb

require_gitmodules:
  tree: require_root
  child: gitmodules (Blob)
  name: .gitmodules

require_lib:
  tree: require_root
  child: require_lib (Tree)
  name: lib

require_lib_ghost:
  tree: require_lib
  child: lib_ghost (Tree)
  name: ghost

require_lib_markdpwn:
  tree: require_lib
  child: markdpwn_012 (Submodule)
  name: markdpwn

require_lib_ghost_rb:
  tree: require_lib
  child: lib_ghost_rb (Blob)
  name: ghost.rb
