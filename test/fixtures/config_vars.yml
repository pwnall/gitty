admin_email:
  name: admin_email
  value: admin@gitty.org

app_uri:
  name: app_uri
  value: "http://localhost:29121/"

git_user:
  name: git_user
  value: git-test

ssh_host:
  name: ssh_host
  value: localhost

signup_email_check:
  name: signup.email_check
  value: enabled
